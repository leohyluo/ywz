<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.alpha.server.rpc.diagnosis.pojo.HospitalInfo" >

    <select id="queryByHospitalCode" resultType="DataRecord" parameterType="HashMap">
        SELECT * from hospital_info WHERE hospital_code = #{hospitalCode}
    </select>
</mapper>