<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.alpha.treatscheme.pojo.DiagnosisDiseaseTreatoptions">

    <select id="getByDiseaseCode" parameterType="HashMap" resultType="DataRecord">
        select * from diagnosis_disease_treatoptions where disease_code=#{diseaseCode} and IFNULL(operate_type,'I') != 'D' order by default_order
    </select>
   

</mapper>